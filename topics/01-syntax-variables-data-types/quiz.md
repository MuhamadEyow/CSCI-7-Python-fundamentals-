# Quiz: Syntax, Variables, and Data Types

1. Which data type stores whole numbers?
2. What is the difference between `"10"` and `10`?
3. What value represents the absence of data?
4. What will `type(3.5)` return?
5. Is `student-name` a valid variable name? Explain why or why not.

## Predict the Output

```python
score = "90"
bonus = 5
print(int(score) + bonus)
```

Write your answers before running any code.
